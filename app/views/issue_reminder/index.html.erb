<h2><%= l(:label_issue_reminder) %></h2>

<% if @issues.any? %>
  <p><%= l(:text_issues_to_remind, count: @issues.count) %></p>
  <table class="list issues">
    <thead>
      <tr>
        <th>#</th>
        <th><%= l(:field_subject) %></th>
        <th><%= l(:field_assigned_to) %></th>
        <th><%= l(:field_due_date) %></th>
      </tr>
    </thead>
    <tbody>
      <% @issues.each do |issue| %>
        <tr>
          <td><%= link_to issue.id, issue_path(issue) %></td>
          <td><%= issue.subject %></td>
          <td><%= issue.assigned_to %></td>
          <td><%= format_date(issue.due_date) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= button_to l(:button_send_reminders), project_issue_reminder_send_reminders_path(@project), method: :post, class: 'button-positive' %>
<% else %>
  <p class="nodata"><%= l(:label_no_data) %></p>
<% end %>
