<h2><%= l(:label_issue_reminder) %></h2>

<% if @issues.any? %>
  <table class="list issues">
    <thead>
      <tr>
        <th>#</th>
        <th><%= l(:field_subject) %></th>
        <th><%= l(:field_assigned_to) %></th>
        <th><%= l(:field_updated_on) %></th>
      </tr>
    </thead>
    <tbody>
      <% @issues.each do |issue| %>
        <tr>
          <td><%= link_to issue.id, issue_path(issue) %></td>
          <td><%= issue.subject %></td>
          <td><%= issue.assigned_to %></td>
          <td><%= format_time(issue.updated_on) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= button_to l(:button_send_reminders), send_reminders_project_issue_reminder_path(@project), method: :post, class: 'button-positive' %>
<% else %>
  <p class="nodata"><%= l(:label_no_data) %></p>
<% end %>
